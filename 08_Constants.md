# Constants

```
// before - after

"+" - ADD                                           // Читаемость, переиспользование арифметической операции 
"-" - SUBTRACT                                      // Читаемость, переиспользование арифметической операции 
"*" - MULTIPLY                                      // Читаемость, переиспользование арифметической операции 
"/" - DIVIDE                                        // Читаемость, переиспользование арифметической операции 

PATH - EXCEPTION_MESSAGES_PATH                      // Улучшена читаемость, возможность переиспользовать в тесте
                                                    // константа характеризует путь до бандла с сообщениями локализо-
                                                    // ванных исключений

TIMEOUT - CONNECTION_TIMEOUT_MS                     // Улучшена читаемость, возможность переиспользовать в тесте
                                                    // константа характеризует сущность дефолтной локали

RU - DEFAULT_LOCALE                                 // Улучшена читаемость, возможность переиспользовать в тесте
                                                    // константа характеризует сущность дефолтной локали
                                                    
API_KEY - API_KEY_HTTP_HEADER                       // Улучшена читаемость, возможность переиспользовать в тесте
                                                    // константа характеризует сущность имени кастомного HTTP заголовка
                                                    
"quiz.answer" - QUIZ_ANSWER                         // Улучшена читаемость, возможность переиспользовать в тесте
                                                    // константа характеризует сущность единицы кода сообщения
 
"id: %d name: %s" - ENTITY_PRINT_FORMAT             // Улучшена читаемость, возможность переиспользовать в тесте
    
1L - SINGLE_BIT                                     // Отказ от магического числа, переиспользование в тесте
                                                    // константа характеризует сущность единицы в битовом представлении

540 - UNSUCCESSFUL_OPERATION                        // Отказ от магического числа,
                                                    // константа характеризует сущность HTTP статуса

5 - DEFAULT_PAGE_SIZE                               // Отказ от магического числа, переиспользование в тесте

0 - DEFAULT_PAGE_NUMBER                             // Отказ от магического числа, переиспользование в тесте
```